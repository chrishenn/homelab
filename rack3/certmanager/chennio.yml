apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: default-chennio-cert
  namespace: default
spec:
  secretName: chennio-cert
  issuerRef:
    name: letsencrypt-dns01-issuer
    kind: ClusterIssuer
  commonName: chenn.io
  dnsNames:
    - chenn.io
    - '*.chenn.io'
