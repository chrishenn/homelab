---
services:
  pgadmin:
    profiles: [pgadmin]
    image: dpage/pgadmin4
    container_name: pgadmin
    restart: unless-stopped
    environment:
      PGADMIN_DEFAULT_EMAIL: ${EMAIL}
      PGADMIN_DEFAULT_PASSWORD: ${PASS}
      PGADMIN_CONFIG_ENHANCED_COOKIE_PROTECTION: True
      PGADMIN_CONFIG_LOGIN_BANNER: '"Chicken PgAdmin"'
      PGADMIN_CONFIG_CONSOLE_LOG_LEVEL: 10
    networks: [traefik]
    labels:
      homepage.group: Development Tools
      homepage.name: PgAdmin
      homepage.icon: pgadmin.png
      homepage.href: https://pgadmin.henn.dev
      homepage.description: Postgres GUI

      traefik.enable: true
      traefik.http.routers.pgadmin.rule: Host(`pgadmin.henn.dev`)
      traefik.http.routers.pgadmin.entrypoints: websecure
      traefik.http.routers.pgadmin.middlewares: hdrs@file
      traefik.http.routers.pgadmin.tls.certresolver: cf
      traefik.http.services.pgadmin.loadbalancer.server.port: 80
