---
services:
  ittools:
    profiles: [ittools]
    image: corentinth/it-tools:latest
    container_name: ittools
    restart: unless-stopped
    networks: [traefik]
    labels:
      traefik.enable: true
      traefik.http.routers.ittools.rule: Host(`ittools.henn.dev`)
      traefik.http.routers.ittools.entrypoints: websecure
      traefik.http.routers.ittools.middlewares: hdrs@file
      traefik.http.routers.ittools.tls.certresolver: cf
      traefik.http.services.ittools.loadbalancer.server.port: 80
      homepage.instance.private.group: Code
      homepage.instance.private.name: IT-Tools
      homepage.instance.private.icon: it-tools.png
      homepage.instance.private.href: https://ittools.henn.dev
      homepage.instance.private.description: Peripheral Code Calculators
