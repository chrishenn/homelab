---
services:
  chartdb:
    profiles: [chartdb]
    image: ghcr.io/chartdb/chartdb:latest
    container_name: chartdb
    restart: unless-stopped
    networks: [traefik]
    labels:
      traefik.enable: true
      traefik.http.routers.chartdb.rule: Host(`chartdb.henn.dev`)
      traefik.http.routers.chartdb.entrypoints: websecure
      traefik.http.routers.chartdb.middlewares: hdrs@file
      traefik.http.routers.chartdb.tls.certresolver: cf
      traefik.http.services.chartdb.loadbalancer.server.port: 80
      homepage.instance.private.group: Code
      homepage.instance.private.name: Chartdb
      homepage.instance.private.icon: chart-db.png
      homepage.instance.private.href: https://chartdb.henn.dev
      homepage.instance.private.description: Database Doodler
