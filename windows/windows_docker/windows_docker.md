# windows_docker

windows containers running on docker on a windows host

enable windows containers, if not already enabled during your docker install
I think there's a pwsh script for this in chrishenn/unattend
