# ref

Reference configs + snippets for projects and deployments that don't fit other categories.

As in, stuff like:

- not deployed to one machine
- doesn't require a dedicated python env
- not restricted to a single OS
